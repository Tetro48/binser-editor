function love.conf(t)
    t.identity = "binser Editor"
    t.window.title = "Editor for bakpakin's Binary Serializer"
    t.window.width = 1600                -- The window width (number)
    t.window.height = 900               -- The window height (number)
    t.console = true
end